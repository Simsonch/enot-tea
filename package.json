{
    "name": "enot-tea",
    "private": true,
    "packageManager": "pnpm@10.33.0",
    "engines": {
        "node": ">=24 <25"
    },
   "scripts": {
    "start:backend:dev": "pnpm --filter @enot-tea/api start:dev",
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  }
}